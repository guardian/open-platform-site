Open Platform Site
==================

A simple HTML site for the Open Platform.


Running locally
---------------

Install Gulp:

```bash
npm install -g gulp
```

Run Gulp:

```bash
cd build
gulp
```
